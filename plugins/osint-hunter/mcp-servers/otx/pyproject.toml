[project]
name = "osint-hunter-otx"
version = "0.1.0"
description = "AlienVault OTX MCP server for osint-hunter"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
